const routes = {
  login: '/login',
  register: '/register',
  dashboard: '/dashboard',
  tasks: '/tasks',
  task: '/task/:id',
  socketenter: '/socketenter',
  socketreceive: '/socketreceive'
};

module.exports = routes;